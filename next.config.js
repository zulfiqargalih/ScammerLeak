module.exports = {
  reactStrictMode: true,
  // Optional: configure image domains if needed
  images: {
    domains: [],
  },
  // Environment variables are injected by Vercel via .env.local
  // No additional config needed here.
};
